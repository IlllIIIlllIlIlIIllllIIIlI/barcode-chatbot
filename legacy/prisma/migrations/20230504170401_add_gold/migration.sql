BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Card] ADD [isGold] BIT NOT NULL CONSTRAINT [Card_isGold_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
